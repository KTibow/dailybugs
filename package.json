{
  "name": "daily-bugs",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "cf-typegen": "wrangler types",
    "format": "prettier . --write"
  },
  "devDependencies": {
    "prettier": "^3.8.1",
    "typescript": "^5.5.2",
    "wrangler": "^4.61.1"
  }
}
